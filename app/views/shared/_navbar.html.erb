<nav class="navbar navbar-expand-lg navbar-light gradient-custom border">
  <div class="container">
    <a class="navbar-brand" href="/">
      <%= image_tag 'icons/building-columns-solid.svg', class: "d-inline-block align-top", style: "width: 40px; height: 40px;" %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Главная</a>
        </li>
        <% if current_user %>
          <% if current_user.is_admin %>
            <li class="nav-item">
              <a class="nav-link active" href="/queries">Запросы</a>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to 'Выйти', logout_path, method: :delete, class: 'nav-link text-danger' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Войти', log_in_path, class: 'nav-link text-success' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Зарегистрироваться', sign_up_path, class: 'nav-link text-primary' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<style>
    .gradient-custom {
        background: linear-gradient(to right, #ff7e5f, #feb47b);
        color: white;
    }

    .navbar {
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        padding: 1rem;
    }

    .navbar-brand {
        font-size: 1.5rem;
        font-weight: bold;
    }

    .nav-link {
        transition: color 0.3s, transform 0.3s;
        font-size: 1rem;
        padding: 0.5rem 1rem;
    }

    .nav-link:hover {
        color: #ffeb3b;
        transform: scale(1.1);
    }

    .navbar-toggler {
        border: none;
    }

    .navbar-toggler-icon {
        background-color: #fff;
    }

    .navbar-collapse {
        justify-content: flex-end;
    }
</style>
